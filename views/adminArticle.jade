extends layout
block page
  - var menu = 'adminArticle'
  include includes/menuArticles
block alert
  include includes/alert
block content
  script.
    var maxPages= #{maxPages};
    var srcPages= '/articlesPageAdmin/';
    var anchorPages = '#articlesAnchor';
    var offsetPages = 10;
    var tag= "#{tag}";
    var category= "#{category}";
    var country= "#{country}";
  script(src='/js/paging.js?v=#{appVersion}')
  script(src="/js/adminArticle.js?v=#{appVersion}")
  div.adminArticle  
    if (articles.length === 0 )
      p
        | Немає статей. Додайте 
        a(href="/postArticle") нову
        |.
    else
      include includes/gridArticlesAdmin